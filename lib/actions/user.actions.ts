'use Server';

export const signIn = async () => {
  try {
    // Mutation / Database / Make Fetch  Ussually POST requests
  } catch (error) {
    console.log('Malakia:', error)
  }
}

export const signUp = async () => {
  try {
    // Create a User account
  } catch (error) {
    console.log('Malakia:', error)
  }
}